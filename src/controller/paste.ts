import type{PasteBlock}from"../core/types";export const PASTE_VISIBLE=5;export function scrollPaste(block:PasteBlock,delta:number):PasteBlock{const maxStart=Math.max(0,block.lines.length-PASTE_VISIBLE);return{...block,visibleStart:Math.min(maxStart,Math.max(0,block.visibleStart+delta))}}export function visibleLines(block:PasteBlock):string[]{return block.lines.slice(block.visibleStart,block.visibleStart+PASTE_VISIBLE)}