= shell-context-demo
:description: Web mockup ? shell context chrome (tokens, left-arrow focus, paste preview, themes).

link:https://hci-nerdz.github.io/docs/hci-nerdz/shell-context-chrome.html[Shell context chrome] ?
link:https://openshellorg.github.io/docs/shell-architecture/persistent-shell-context.html[Persistent shell context @ OpenShellOrg]


== Layout demos (2026-08-28)

Live: link:https://hci-nerdz.github.io/shell-context-demo/[hci-nerdz.github.io/shell-context-demo]

[cols="1,2",options="header"]
|===
| Route | What it shows

| `#/prompt`
| Existing shell context chrome desk

| `#/standalone`
| Top / vertical tabs + shared surface

| `#/decoupled`
| Index chrome + separate session windows

| `#/manager`
| projectGroupedManager + DevCentr grid highlight/dull

| `#/zones`
| Contained tiling / nested zones (faded empty)

| `#/association`
| Subscriber + session registration mock
|===

Canon: link:https://github.com/openshellorg/shell-architecture[shell-architecture],
link:https://github.com/HCI-Nerdz/docs/blob/main/docs/modules/ROOT/pages/contained-tiling.adoc[contained-tiling].

== Run locally

[source,bash]
----
pnpm install
pnpm dev
----

Opens http://localhost:5175/ (Vite dev server).

== Demo controls

* **Left-arrow** ? cycle token focus (path ? user ? host ? app tokens)
* **Right-arrow / Enter** ? return toward input
* **PgUp / PgDn** ? scroll paste preview (5 visible lines)
* **Theme** button ? blue/grey default vs high-contrast
* **?** gutter ? static prohelp placeholder (no backend)

== Deploy

Live *repo* Pages (this repo’s Actions workflow publishes `dist/`):
link:https://hci-nerdz.github.io/shell-context-demo/[https://hci-nerdz.github.io/shell-context-demo/]

Vite `base` is `/shell-context-demo/` so assets resolve under that project slug.

Later: a catalog card on the org site at
link:https://hci-nerdz.github.io/demos/shell-context/[hci-nerdz.github.io/demos/shell-context/]
(HCI-Nerdz.github.io Astro — not wired in this pass).

[source,bash]
----
pnpm build
----

== Typography

Prompt/cell mock uses the Open Shell curated monospace stack (system fonts only — no bundled faces):

[source,css]
----
font-family: "Consolas", "Cascadia Mono", "Courier New", monospace;
----

**Consolas** is the shipping default to fit OSO chrome visual language; users may override later in the real shell. Spec: link:https://openshellorg.github.io/docs/shell-architecture/typography.html[typography @ shell-architecture].

== Architecture

`src/core/` ? `src/controller/` ? `src/render/` ? see HCI Nerdz prototype-strategy.

== Changelog

See link:CHANGELOG.adoc[CHANGELOG.adoc].

== License

MIT ? see link:LICENSE[LICENSE].
