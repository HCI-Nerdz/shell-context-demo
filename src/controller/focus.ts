export function tokenFocusOrder(tokenIds:string[]):string[]{const host=["path","host","user"];const app=tokenIds.filter(id=>!host.includes(id)).reverse();return[...host.filter(id=>tokenIds.includes(id)),...app]}export function stepFocus(order:string[],current:string|null,delta:-1|1):string|null{if(!order.length)return null;if(current===null)return delta===-1?order[0]!:null;const idx=order.indexOf(current);if(idx===-1)return order[0]!;const next=idx+delta;return next<0||next>=order.length?null:order[next]!}