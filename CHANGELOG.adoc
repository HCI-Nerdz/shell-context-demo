= Changelog

== 2026-08-30 — Identity strip is site nav only

* Hub breadcrumb `HCI-Nerdz / shell-context-demo`: org → demos index, repo → this demo's site home (not GitHub).
* GitHub remains only on the VCS logo line under the strip.

== 2026-08-30 — OpenShellOrg links use opensh.org

* README OpenShellOrg docs links use https://opensh.org/ instead of openshellorg.github.io.

== 2026-08-28 -- Intro anchor mockup

* Suite lede includes exactly one anchoring Open Shell facsimile screenshot (`public/intro-anchor.png`, mirrored under `screenshots/`) before the variant switcher.
* Captured from `tools/intro-mock.html` (Consolas stack + host-chrome context tokens) for a single memory-hook before the interactive desks.

== 2026-08-28 -- Facsimile windowing + hub chrome

* Hub chrome: org/repo identity strip, GitHub VCS line, suite lede, variant switcher, per-variant title + lay blurb.
* Zones: delete zone (toolbar + title-bar close), minimize, drag, resize, z-order focus on facsimile desk.
* Decoupled: faux windows with drag / focus / min / close / resize.
* README advisory: facsimile desks vs real OS windowing.
* UTF-8 sources (nav/windowing).

== 2026-08-28 -- Layout mode demos

* Hash routes: `#/standalone`, `#/decoupled`, `#/manager`, `#/zones`, `#/association` (prompt desk remains `#/prompt`).
* Interactive mocks for Open Terminal layout modes + DevCentr grid highlight/dull + nested zones + association.
* Screenshots under `screenshots/` used by shell-architecture / HCI docs.

== 2026-08-28 -- Typography

* Prompt/cell mock font stack: Consolas to Cascadia Mono to Courier New to monospace (Open Shell curated default).

== 2026-08-28 -- GitHub Pages

* Actions workflow publishes `dist/` to project Pages at `/shell-context-demo/`
* Vite `base` matches that slug; org-site `/demos/shell-context/` catalog card is still later

== 2026-08-28

* Wave 1 scaffold.
